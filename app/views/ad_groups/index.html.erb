<p id="notice"><%= notice %></p>

<h1>Ad Groups</h1>

<table>
  <thead>
    <tr>
      <th>Campaign</th>
      <th>Name</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @ad_groups.each do |ad_group| %>
      <tr>
        <td><%= ad_group.campaign %></td>
        <td><%= ad_group.name %></td>
        <td><%= link_to 'Show', ad_group %></td>
        <td><%= link_to 'Edit', edit_ad_group_path(ad_group) %></td>
        <td><%= link_to 'Destroy', ad_group, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Ad Group', new_ad_group_path %>
